apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  labels:
    app: demo-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
        - name: demo-app
          image: PLACEHOLDER_IMAGE
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1"
          env:
            - name: ENV
              value: prod
            - name: LOG_LEVEL
              value: ERROR
            - name: FEATURE_FLAG
              value: "false"
            - name: IMAGE_TAG
              value: PLACEHOLDER_IMAGE
